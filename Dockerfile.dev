# Development-only Dockerfile. Production deploys to Vercel, not this image
# (see README.md's "Local Development vs. Production" section).

FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm install

COPY . .

EXPOSE 3000

CMD ["npm", "run", "dev"]
