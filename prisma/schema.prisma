// E-Commerce Platform v1 — Prisma schema.
// Phase 0: datasource + generator configuration only. Models are added in Phase 1
// per the entities defined in docs/architecture.md Section 2.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
