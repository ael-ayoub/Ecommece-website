// Re-exported from the provider so every consumer imports from the
// project's single canonical hooks location (src/hooks/), while the
// Context/reducer implementation stays co-located with the other providers.
export { useCart } from "@/components/providers/CartProvider";
